import fetch from 'node-fetch';

const run = async () => {
  try {
    // We would need a valid token to test the API.
    // Instead of hitting the API directly, let's just create a mock order, save it, populate it, and log it to be 100% sure.
  } catch (err) {
    console.error(err);
  }
};
run();
